#include "bits/stdc++.h"
using namespace std;
int main(int argc, char const *argv[]) {
  int item_num;
  int least_item,up,bot;
  int is_continue=0;
  while (is_continue==0) {
    cin>>item_num>>least_item>>up>>bot;

    cin>>is_continue
  }
  return 0;
}
